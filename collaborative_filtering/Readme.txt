The script can be run from the command line:

python cf.py  --train TrainingRatings.txt --test TestingRatingsMedium.txt

This script takes a while to run on the data provided. A simpler example would be to use the tiny training and tiny testing files:

python cf.py  --train tinyTraining.txt --test tinyTesting.txt
